<h1><%= @task.title %></h1>
<%= link_to "Edit task", edit_task_path %>

<p><%= @task.details%></p>
  <p><input type="checkbox" <%= @task.completed ? "checked": "" %>>
  <label for="checkbox"><%= @task.completed ? "This task is completed": "This task is not completed" %></label></p>

<%= link_to "Back to list", tasks_path %>
